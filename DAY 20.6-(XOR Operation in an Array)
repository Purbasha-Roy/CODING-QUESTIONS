//XOR Operation in an Array
int xorOperation(int n, int start) {
        int ans=start;
        int current=start;
        for(int i=1;i<n;i++){
            current=start+2*i;
            ans=ans^current;
        }
        return ans;
    }
