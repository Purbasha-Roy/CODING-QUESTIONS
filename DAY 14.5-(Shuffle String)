//Shuffle String
    string restoreString(string s, vector<int>& indices) {
        string output=s;
        for(int i=0;i<s.size();i++){
            int index=indices[i];
            output[index]=s[i];
        }
        return output;
    }
